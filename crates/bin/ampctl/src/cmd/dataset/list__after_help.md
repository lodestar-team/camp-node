## Examples

List all registered datasets:
  $ ampctl dataset list

Using the `ls` alias:
  $ ampctl datasets ls

## Output

Returns JSON array with dataset summaries including:
- Namespace and name
- Latest version tag (if any)
- All available semantic versions