//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: false,
  trailingComma: "es5",
  printWidth: 120,
  tabWidth: 2,
  arrowParens: "always",
}

export default config
