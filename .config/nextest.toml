[[profile.default.overrides]]
filter = 'test(postgres)'
threads-required = "num-test-threads"

[[profile.default.overrides]]
filter = 'package(ampsync)'
threads-required = "num-test-threads"

[[profile.default.overrides]]
filter = 'package(metadata-db)'
threads-required = "num-test-threads"

[profile.local]
# Exclude tests that require external dependencies
default-filter = '''
  all() -
  test(eth_beacon_single_dump) -
  test(eth_firehose_single_dump) -
  test(evm_rpc_single_dump) -
  test(evm_rpc_base_single_dump) -
  test(sql_tests) -
  test(sql_dataset_input_batch_size) -
  test(intra_deps_test) -
  test(multi_version_test) -
  test(streaming_tests_with_sql_datasets) -
  test(base_firehose_single_dump) -
  test(install_already_installed_version_switches_to_it) -
  test(install_latest_version) -
  test(install_specific_version) -
  test(extractor::tests::historical_to_json_rpc_transition)
'''
